# SQLアンチパターン

SQLアンチパターンを読み、その内容や感想などを記録として残す。

※ 韓国語版で読書したため、日本語版での用語や構成と一部差異がある可能性がある。

## 各章ごとの内容まとめ・個人的なメモ

### 第1章
#### アンチパターンとは？
アンチパターンとは、問題を解決するために一連の手法を実行したものの、結果として別の問題を引き起こしてしまう技術や方法を意味する。

#### アンチパターンのタイプ
1. 論理データベース設計のアンチパターン: テーブル・カラムなど、DBの論理的な構造を設計する際に発生するアンチパターン。
2. 物理データベース設計のアンチパターン: インデックス・ストレージなど、データを実際に保存・管理する仕組みを設計する際に発生するアンチパターン。
3. クエリのアンチパターン: SQLクエリの記述方式・データ取得と操作方法に関するアンチパターン。
4. アプリケーション開発のアンチパターン: DBとアプリケーションを連携する際の実装や設計に関するアンチパターン。

---

> **論理データベース設計のアンチパターン**
>
> 第2章〜第9章では、論理データベース設計に関するアンチパターンを扱う。
### 第2章　ジェイワーク(信号無視)

#### もともと達成したかった目標
1つの属性に複数の値を保存したい。

例）1つの商品に複数のタグを付けるなど、1対多や多対多の関係を持つデータを管理したい場合

#### アンチパターン
複数の値をカンマなどの区切り文字で連結し、1つのカラムに値を保存するパターン。

例) 1つの商品に複数のタブが紐づく場合、次のような対応を行った。

product_id | tag_ids
---------- | -------
1          | 1,3,5
2          | 2,4

#### なぜ問題なのか
テーブルを増やさなくて済むので実装しやすいが、データベースが本来持っているリレーションの仕組みをまったく利用できない。

1. 値を取得するクエリが複雑になる
- カラム内に複数の値が保存されるので、特定の値を検索する際に文字列検索などのクエリが別途必要になる。
2. インデックスを活用しにくい
- 複数の値として保存され、1つの値に対して適切にインデックスを利用するのは不可になる。
3. 値の追加・削除が複雑になる
- 新しい値の追加や削除が、文字列の操作的な感じになってしまう。
4. 区切り文字の選択に問題が発生する可能性がある
- 区切り文字自体をデータとして保存したいケースでは衝突が発生し、挙動に問題が発生する恐れがある。
5. 保存できる値の数に制限がある
- カラムの最大長によって保存できる値の数が制限される。

#### このパターンが適切な場合は？
このパターンは非正規化の一種で、特定のケースではクエリのパフォーマンスを向上させるために利用できる余地がある。

例）関係自体をDBで操作する必要がない、ただ保存されているリストそのものをそのまま読み取るケース

#### 解決策
多対多の関係を持つデータに関しては、中間テーブル(交差テーブル)を作成し、それぞれの値を別々の行として管理するようにする(正規化)。

例）商品とタグの関係を反映して3つのテーブルを作成し管理する
product_id | ...
---------- | -------
1          | ...
2          | ...

tag_ids | ...
------- | -------
1       | ...
3       | ...
5       | ...

product_id | tag_id
---------- | ------
1          | 1
1          | 3
2          | 5

これにより、先程の問題を以下のように解決できる
- 複雑な検索：`JOIN`を利用して、個々の値を通常の条件として検索可能
- インデックスの利用：個々の値を別々の行として管理するのでインデックスが効く
- 値の追加・削除：文字列の操作が不要になり、`INSERT`, `DELETE`として処理するのでRDBMS的な利用が可能になる
- 区切り文字の問題：区切り文字をそもそも指定する必要がなくなる
- 保存できる値の数の制限：文字列の長さなどの制限がなくなったので、値の数に応じて保存できる

#### 🤔 個人的なメモ
このパターンの例外ケースが紹介されているが、現実正解ではほとんど存在しないと思うので、ちゃんと正規化してRDBMSの機能を最大限活用できるようにするのがそもそも良いと思った。

### 第3章　ナイーブツリー(素朴な木)

#### もともと達成したかった目標
階層構造を持つデータをデータベースに保存し、階層関係を持ったまま検索・管理したい

例）コメントに値する返信のように、1つのデータが別のデータの子要素になる構造
```
コメントA 
├── コメントB 
│ 　├── コメントD 
│ 　└── コメントE 
└── コメントC 
　　└── コメントF
```

#### アンチパターン
各ノードが自分の親ノードのIDだけを保存する形(隣接リスト)を使用して、階層構造を管理するパターン。

例) コメントの返信関係を以下のように保存する
comment_id | parent_id | comment 
-----------|-----------|--------- 
1 | NULL | A 
2 | 1 | B 
3 | 1 | C 
4 | 2 | D 
5 | 2 | E 
6 | 3 | F

#### なぜ問題なのか
上記の方法では、各コメントが直接の親だけを知っている。
隣接リスト自体は単純でデータの追加・直接の親と子の取得もしやすいが、階層全体や深い階層にあるデータの取得などには問題が発生しえる。

1. 深い階層のデータを取得するクエリが複雑になる
- 1回の `JOIN` では直接の親と子の関係しか取得できないので、階層の深さに応じて `JOIN` を追加していくしかない
2. 階層の深さを事前に把握する必要がある
- 階層が何段階まで続くか分からない場合、そもそも `JOIN` の回数を決めること自体ができない
3. ツリー全体の取得が難しい
- 特定のノードからすべての子孫を取得したい場合、複雑なクエリを使うしかない。
4. データベースによっては再帰クエリが利用できない
- `WITH RECURSIVE` などの再帰クエリが利用可能なDBでは隣接リスト方式でも階層構造を取得できるが、そのような機能を提供しないDBもあり、将来のメンテナンス時に困難になる場合がある。

#### このパターンが適切な場合は？
階層構造の回数が事前に定まっていて、構造自体が浅い場合は隣接リストでも問題ない可能性がある。

例）必ず1階層までしか存在せず、「親を取得する」・「子を取得する」といった操作だけで済む場合など

#### 解決策
隣接リスト以外のツリーモデルを利用する

例1）経路列挙
各ノードにルートから現在のノードまでの経路を保存する方式
node | path 
-----|------------ 
A | /A/ 
B | /A/B/ 
D | /A/B/D/

例2）入れ子集合
各ノードに自分の子のセット(左・右)の値をもたせ、値の範囲によって階層関係を表現する方式
node | lft | rgt
-----|-----|-----
A    | 1   | 12
B    | 2   | 7
D    | 3   | 4
E    | 5   | 6
C    | 8   | 11
F    | 9   | 10

例3）クロージャーテーブル
ノード同士の祖先・子孫関係を別のテーブルに保存する方式
ancestor | descendant 
---------|----------- 
A | A 
A | B 
A | C 
A | D 
B | B 
B | D

これにより、先程の問題を以下のように解決できる
- 深い階層の取得：階層の深さに応じて `JOIN` の回数を追加する仕組みが不要になる。
- 階層全体の取得：隣接リストに比べて、階層全体の取得クエリが用意になる。
- データベースの制約：再帰クエリを使わなくても設計自体で解決できるので、特定のDBに依存することがなくなる。

#### 🤔 個人的なメモ
そもそも深い階層まで管理するデータ構造を構築したことがあまりないが、階層構造をどのように検索するかまで考えると、データの保存方法だけでなく、読み取り方も含めて設計する必要があることを学んだ。

### 第4章　IDリクワイアド(とりあえずID)

#### もともと達成したかった目標
データを一意に識別できるようにし、他のデータから特定の1行を参照・更新・削除したい

例）ユーザやコメントなどの各レコードにIDを持たせる

| id | name |
|----|------|
| 1  | A    |
| 2  | B    |
| 3  | C    |

#### アンチパターン
とりあえず全てのテーブルに対してid列を追加し、そのidさえあればレコードを一意に識別できると思ってしまうパターン。

例) 本来 email が一意であるべきなのに、id を指定してしまう

| id | email            |
|----|------------------|
| 1  | example@test.com  |
| 2  | example@test.com  |

#### なぜ問題なのか
自然キーや複合キーで十分表現できるのに機械的に代理キーとして追加してしまってるので、以下のような問題が発生しえる。

1. 本来必要な一意性をidだけでは保証できない
- 代理キーのidは各行を識別するだけなので、業務上同じ値が存在してはいけないという制約までは表現できない
- 例えば、メールアドレスは一意性を保証すべきカラムなのに、id を別指定してしまうとDB上制約がなくなる
2. 複合的な一意性が守れられなくなる
- 中間テーブルなど、複数のカラムの組み合わせによって一意になる場合がある
- id追加の対応だけでは、その組み合わせがユニークであることを表現できない

#### このパターンが適切な場合は？
例えば、以下のケースでは代理キーを付けた方が良い場合がある。
- 自然キーが存在しない
- 自然キーが長い・または変更される可能性がある
- キーが複雑すぎて、他のテーブルから参照しにくい
- アプリケーション実装時にフレームワークを利用していて、そのフレームワーク上代理キーで扱うのが楽な時

#### 解決策
自然キーと複合キーを見つけ、それを利用するようにする。
- テータがもつ自然な一意性を利用するので、テーブル定義上で仕様や制約を把握しやすくなる
- 複数のカラムの組み合わせをキーとして利用することで、その組み合わせの一意性を担保できるようになる

### 第5章　キーレスエントリ(外部キー嫌い)

#### もともと達成したかった目標
データベースの構造をできるだけシンプルにし、外部キー制約による複雑さを避けたい

#### アンチパターン
外部キー制約を宣言せず、参照整合性をアプリケーションコードだけで保証しようとするパターン。

例) テーブル間の関係で外部キー制約を設定せず、アプリケーションコード上で参照先の存在確認や削除時のチェックなどを実装する

例えば、`Bugs.reported_by` カラムが `Accounts.account_id` を参照している場合、本来なら以下のようにFK制約を定義するが、

```
FOREIGN KEY (reported_by)
  REFERENCES Accounts(account_id)
```

上記のFK制約の代わりに、
- INSERTの前に参照先のレコードが存在するかをコードでチェックする
- DELETEの前に参照している子レコードが存在しないかコードでチェックする
- UPDATE/DELETEによって参照切れが発生しないようにコード側で処理する
などを行う行為。

#### なぜ問題なのか
1. アプリケーションコードが完全に正しいことを前提にする必要がある
- FK制約を使わない場合、DB自身は不正な参照を防止できなくなってしまう
- そのせいで、アプリケーション側のチェック処理に不備があったり別のアプリケーションやバッチ処理などからデータが更新されたりすると、参照整合性が簡単に破られてしまう。
2. データの整合性を定期的にチェックする必要がある
- FK制約がないので、参照切れが発生していないかを別途定期的にチェックする必要が出てくる
- この場合、チェック頻度をどうするか、チェック処理によるDBへの負荷はどのぐらいなのか、不整合が発覚した場合にどのように対応するかなどを考える必要があり、運用コストも増えてしまう
3. 「自分の責任ではない」という問題が発生する
- 万が一、DB上で不整合が発生した際に、その原因がアプリケーションコードにあるのか、バッチ処理にあるのかなど、原因を特定するのが難しくなる
- FK制約の設定をしておけば、少なくとも参照整合性を満たさない変更は検知できるので問題を予防できる
4. 更新時にCatch-22が発生する
- 親を先に更新すると子が不正になり、子を先に更新すると親が存在しないという、どうやっても先に進めない問題を意味
- この問題を防ぐために一部の人はFK制約を使わない
- ただ、Catch-22問題は外部キー制約自体の問題ではなく、CASCADE などの機能を利用することで解決できる場合がある

#### このパターンが適切な場合は？
- 使用しているデータベースがFK制約機能をサポートしていない場合
- 非常に大規模なシステムで、FK制約によるロックやパフォーマンスへの影響を慎重に検討する必要がある場合

#### 解決策
参照整合性をデータベースに保証させるために、外部キー制約を宣言する。
- 参照整合性を保証するためのアプリケーション側のチェック処理を減らすことができる
- そもそもデータの作成・更新・削除でFK制約により整合性が担保されるので、データの整合性を定期的にチェックする手間がほとんどなくなる
- 上記の同様な理由で、作成・更新・削除などで不整合のデータがDBに存在してしまう問題を予防できる

#### 🤔 個人的なメモ
RDBを利用しているのであれば、テーブル間の参照整合性は基本的にDB側で保証する方針が良さそう。
ただし、アプリケーション側も整合性チェックの責任を担う必要があるかもしれないので(例えばクライアントに重複や存在チェックに関するエラーを返したいとき等)、状況に応じて柔軟な対応が必要だと思った。

### 第6章　EAV(エンティティ・アトリビュート・バリュー)

#### もともと達成したかった目標
将来的に変化が予想される属性を、柔軟に管理できるようにしたい

例えば、PCとスマホは同じ電子機器だが、その製品が持っている属性は異なる。
- PC: CPU, メモリ容量・グラフィックボード
- スマホ: 画面サイズ・カメラ画素数・バッテリー容量

上記のように扱うデータの種類によって必要な属性が異なる場合、通常では属性が増えるたびにカラムを追加する必要が出てくる。

そこで、属性をカラムとして定義するのではなく、属性そのものをデータとして扱えば、どんな属性でも追加できるのではないかと考えて、これをもとに柔軟なデータ構造を実現しようとする。

#### アンチパターン
属性をカラムとして定義する代わりに、属性名と値を行として保存する汎用的な属性テーブルを作るパターン。
上記のことをEAVという。

通常のテーブルとEAVを採用したテーブルの違いは以下である。

- products(通常のテーブル管理)

| product_id | name | cpu | memory |
|------------|------|-----|--------|
| 1          | PC A | i7  | 16GB   |
| 2          | PC B | i5  | 8GB    |

- product_attributes(EAVパターンでの管理)

| product_id | attribute | value |
|------------|-----------|-------|
| 1          | cpu       | i7    |
| 1          | memory    | 16GB  |
| 2          | cpu       | i5    |
| 2          | memory    | 8GB   |

EAVパターンを採用すると、新しい属性が必要になっても元テーブルのカラムを追加する必要がなくなる。

#### なぜ問題なのか
一見するとEAVは柔軟な対応にみえるが、属性はデータとして扱うことで、RDBが持つ色んなメリットを失ってしまう。

1. データ型を適切に定義できない
- カラムごとに管理する通常の方式ではカラムごとに適切なデータ型を定義できるが、EAVでは `value`に該当するカラムは同じ型になるしかない
- そのため、数値か文字列か日付か真偽値なのかをDB上で保証できなくなる
2. 制約を適用しにくい
- 通常の管理方式だとカラム単位で色んな制約をかけることができるが、EAVでは `value`がすべての属性に対して共通なので、勝手に制約をかけることができない
- 制約をDB上で設定できない分、アプリケーションコードでその責任を移譲する必要が出てくる
3. クエリが複雑になる
- EAVパターンでは attribute, value が同じテーブルで管理されているので、ソート・集計・範囲検索・属性の存在チェックなどが全部複雑になりがち
4. テーブルの関係を表現しにくい
- EAVでは value の値が attribute に該当しているかをDB自身では理解できないので、外部キー設定が難しい

#### このパターンが適切な場合は？
属性が増えるかもしれないという理由だけではEAVを選択しない方が良い。
非リレーショナルなデータを扱う必要がある場合には、MongoDBなどのドキュメント志向データベースなど、要件により適した別のデータベースを検討することが重要である。

#### 解決策
EAVを使う前に通常のリレーショナルなテーブルとしてモデル化できないか検討する。
特に、扱う属性やデータの種類がある程度決まっているのであれば、サブタイプごとにテーブルやカラムを定義する方法を検討する。

例えば、

- products

| product_id | name | type |
|------------|------|------|
| 1          | PC A | PC   |
| 2          | Phone A | PHONE |

に加えて、

- pcs

| product_id | cpu | memory |
|------------|-----|--------|
| 1          | i7  | 16GB   |

- smartphones

| product_id | screen_size | camera |
|------------|-------------|--------|
| 2          | 6.5         | 50MP   |

のように、共通する属性とサブタイプ固有の属性を分けて管理すると、RDBのメリットを維持することができる。

### 第7章　ポリモーフィック関連

#### もともと達成したかった目標
1つのテーブルから、複数の種類の親テーブルを参照できるようにしたい

例) コメント機能を実装するとき、コメントを特定の1種類のテーブルだけではなく、投稿・イメージ・動画など、複数のテーブルに対して共通して利用したい場合

#### アンチパターン
1つの外部キーと参照先を示す列を組み合わせて、複数のテーブルを参照するパターン。

例) `comments`から `bugs`、`feature_requests`のどちらにもコメントできるようにしたく、以下のようなテーブルを作る

- comments

| comment_id | issue_type | issue_id | comment |
|------------|------------|----------|---------|
| 1          | BUG        | 10       | ...     |
| 2          | FEATURE    | 20       | ...     |

`issue_id`は`bugs.issue_id`または`feature_requests.issue_id`のどちらかを参照し、`issue_type`には`issue_id`がどのテーブルを参照しているかを識別するための値を格納する。

そのため、`issue_id`だけでは参照先が判断できないので、外部キーと参照先のテーブル名の列を組み合わせて、1つの外部キーを複数のテーブルへの参照として利用する。

#### なぜ問題なのか
一見すると、1つのテーブルから複数のテーブルを参照できるようにみえるが、RDBが持つ参照整合性が活用できなくなってしまう。

1. 外部キー制約を設定できない
- ポリモーフィック関連になると `issue_id` が `bugs` と `feature_requests` のどちらを参照するのか、行によって異なるので、`issue_id`カラムにたいして通常のFK制約をかけるのは不可能
- そのため、存在しないバグを参照するコメントなど、不整合なデータが存在する恐れがある
2. 参照整合性をアプリケーションコードで管理する必要がある
- `issue_type`と`issue_id`の組み合わせが正しいかをDBでは保証できないので、その責任をアプリコードが担う必要が出てくる
- アプリコードのメンテナンス的に手間がかかってしまう
3. ER図で関係を明確に表現しにくい
- ポリモーフィックな関係になっているテーブル同士をER図で表現するのが難しく、DBの構造や参照関係がわかりにくくなる
4. クエリが複雑になりやすい
- `issue_type`によって参照するテーブルが変わるので、その分SQLクエリ自体が複雑になってしまう可能性がある

#### このパターンが適切な場合は？
Ruby on Railsなど、ポリモーフィック関連をサポートする成熟したORMフレームワークを利用していて、フレームワーク側で関連や整合性を適切に管理可能な場合

#### 解決策
ポリモーフィックな関係を利用するのではなく、DBの参照整合性が保証できるようにデーブル構造や関係性を見直す。

1. 逆方向に参照する

ポリモーフィックな関係では、子テーブルが複数のテーブルを参照しようとしている。
これを逆にして、親テーブル側から子テーブルを参照する構造に逆転させる。

例えば、`comments`から`bugs`や`feature_requests`を直接参照するのではなく、それぞれ専用の中間テーブルを用意することで、通常の外部キーとして参照できるようにする。

- bug_comments(中間テーブル)

| bug_id   | comment_id |
|----------|------------|
| 10       | 1          |

- feature_comments(中間テーブル)

| feature_id | comment_id |
|------------|------------|
| 20         | 2          |

その後、各中間テーブルに対して外部キーを設定する。

2. 共通のスーパーテーブルを作成する

`bugs`、`feature_requests`がどちらも issue という共通の概念を持っているのであれば、`issues`のテーブルを作成する。

- issues(スーパーテーブル)

| issue_id |
|----------|
| 10       |
| 20       |

- bugs

| issue_id | ... |
|----------|-----|
| 10       | ... |

- feature_requests

| issue_id | ... |
|----------|-----|
| 20       | ... |

- comments

|comment_id | issue_id | comment |
|------------|----------|---------|
| 1          | 10       | ...     |
| 2          | 20       | ...     |

`comments`は`bugs`、`feature_requests`を直接参照するのではなく、両者の親になる `issues`を参照する形にすると、`comments.issue_id`に存在しないIDが登録されるのをDB側で防止することができる。

両者の解決策は通常のRDBの動作と同じく参照整合性をDB側で保証できるので、前に言及した問題点を迂回することができる。

#### 🤔 個人的なメモ
とりあえず、RDB自体が提供している機能を最大に活かす方針でDBを設計する必要がある(データの整合性はDB側で責任を負う)。

### 第8章　マルチカラムアトリビュート(複数列属性)

#### もともと達成したかった目標
1つのデータが、同じ種類の属性を複数持てるようにしたい。

例) 1つのバグに複数のタグを設定できるようにしたい場合

#### アンチパターン
1つの属性が複数存在する場合、それぞれを別々のカラムとして定義して管理するパターンのこと。

例) 複数タグを対応するために、以下のように `bugs` テーブルに同じ種類の属性を複数のカラムとして定義して管理する

```
CREATE TABLE bugs (
  bug_id  SERIAL PRIMARY KEY,
  description VARCHAR(1000),
  tag1  VARCHAR(20),
  tag2  VARCHAR(20),
  tag3  VARCHAR(20)
);
```

#### なぜ問題なのか
1. 取得系のクエリが複雑になる
- 上記の例だと、特定のタグを持つバグを取得するために `tag1`、`tag2`、`tag3`をすべて比較するようなSQL文を作成する必要があり、WHERE句が複雑になる
- 複数のタグ名で検索を行う場合は、さらに複雑になる
2. 属性に値を追加・削除する際に、アプリケーション側で追加のチェック処理が必要になる
- 単純な INSERT や DELETE ではなく、現在の値や空いているカラムを確認して、どのカラムを操作するかを判断する処理が必要になる
3. 値の一意性を保証できない
- 特定のバグが持つタグの値が一意なのかをDB側で担保できないので、`tag1`、`tag2`、`tag3`に同じタグが重複して登録される恐れがある。
4. 仕様変更により属性の個数が変わった場合の対応が難しい
- 例えば、現在はバグが最大3つのタグを持つ仕様になっているが、将来4つまで持てるように変更された場合、マイグレーションによって既存テーブルの定義を変更する必要がある
- 加えて、アプリケーション内で該当テーブルを利用しているすべてのコードについて、新しいカラムに対応するようにSQL文などを修正する必要がある 

#### このパターンが適切な場合は？
属性の数が明確に決まっていて、将来的にその数が変わる可能性がほとんどない場合。

#### 解決策
第2章のジェイワークの解決策と同様に、複数の対を別々の行として管理するための専用テーブルを作成する(正規化)。

```
CREATE TABLE tags (
  bug_id  BIGINT UNSIGNED NOT NULL,
  tag VARCHAR(20),
  PRIMARY KEY (bug_id, tag),
  FOREIGN KEY (bug_id) REFERENCES bugs(bug_id)
);
```

上記のようなテーブルを作ることで以下のメリットが得られる。
- 1つのタグはもちらん、複数のタグ名を条件にバグを検索する場合でも、SQL文を比較的簡単に記述できる
- どのカラムの値を更新するかをアプリケーション側で判断する必要がなくなり、自然な INSERT や DELETE で簡単に操作できるようになる
- 1つのバグに同じタグが重複して登録されないことをDB側で保証できる
- バグが持つタグの数に変更があっても、テーブル定義を変更するマイグレーションやアプリケーションコードの変更が不要になる

#### 🤔 個人的なメモ
前のチャプターからの教訓と同じく、柔軟性や扱いやすさだけを優先するのではなく、ちゃんと正規化を行うことでRDBMSが提供する機能を最大限に活用できるように設計することが重要だと感じた。

### 第9章　メタデータトリブル(メタデータ大増殖)

#### もともと達成したかった目標
時間の経過とともに継続的に増えていくデータを効率よく管理し、クエリのパフォーマンスを改善したい。

例) 毎月大量のデータを記録していて、将来もその記録が同様に増えると予想されるケース

#### アンチパターン
連続的に増加するデータを管理するために、同じ構造のテーブル・カラムを必要になるたびに追加していくパターン。

例) 月ごとのデータを保存するために、以下のような運用をやっている

```
CREATE TABLE data_2026_01 (...);
CREATE TABLE data_2026_02 (...);
CREATE TABLE data_2026_03 (...);
...
```

データの増加に合わせてDBスキーマを増殖させていくことをメタデータトリブルと呼ぶ。

#### なぜ問題なのか
1. 複数の期間をまたいで検索する場合など、クエリが複雑になりすぎ
- 検索対象のテーブル数が増えるほど、SQLまたはアプリケーションコードが複雑になる可能性がある
2. 毎回テーブルを作成・メンテナンスするのが複雑
- 定期的に新しいテーブルの定義が必要で、毎回同様なインデックスや制約などを設定する必要がある
- カラムに変更があった場合などで、既存の全てのテーブルに対して一貫性を保つためにマイグレーションが必要になる恐れがある

#### このパターンが適切な場合は？
過去のデータを現在のデータから物理的に分離しアーカイブする場合。

#### 解決策
基本的にDBスキーマで対応するのではなく、RDBMSが提供する機能を活用する。
1. 水平分割
- パーティショニング機能を利用し、物理的には１つのテーブルに扱いながら、内部的にはデータを複数のパーティションに分割して管理する
- データを期間などの条件でパーティション分割をすると、テーブル自体を増やす必要はない
- 特定の期間のデータだけを検索する場合、不要なパーティションは対象から除外できるのでパーフォーマンス的にも改善される可能性がある
2. 垂直分割(パフォーマンス面の考慮)
- １つのテーブルにカラムが多い場合、複数のテーブルを分割する
- 頻繁に利用するカラムと、ほとんど使用しないカラムを分離するなどで対応
3. メタデータトリブルを起こさないようにテーブル設計を見直す
- データが増えるたびにテーブルやカラムを追加するのではなく、基本的に１つのテーブルにデータを追加していくようにする
- 必要に応じて、中間テーブルを用意することで年度ごとのカラムを増やすことを防ぐことが可能

---

> **物理データベース設計のアンチパターン**
>
> 第10章〜第13章では、物理データベース設計に関するアンチパターンを扱う。

### 第10章　ラウンディングエラー(丸め誤差)

#### もともと達成したかった目標
整数ではない数値を保存し、正確な算術計算に利用したい

例) 商品の価格(ドルやユーロ基準)・割合など、小数を含む数値を扱い場合

#### アンチパターン
小数を扱うために、データ型としてFLOATを利用するパターン

例) ドル基準で商品価格を設定するとき
```
CREATE TABLE products (
  product_id BIGINT PRIMARY KEY,
  price FLOAT
);
```

#### なぜ問題なのか
1. 一部の小数は、有限の精度では正確に表現できない
- 例えば 1/3 は 0.333... と無限に続くので、有限の桁数では正確に表現できない
- また、0.1のように10進数では有限の桁数で表現できる値も、２進数の浮動小数点形式では正確に表現できない場合がある
- FLOATでは実際の値に違い値として保存されることがある
2. FLOATを利用すると、比較や算術計算で期待した結果が得られない場合がある
- 保存された値が近似値であるため、計算結果にも誤差が発生する
- また、有限精度の小数値を条件に指定して検索すると、期待した行が取得できない可能性がある
- 特に、金額など値の正確性が重要になるデータでは問題になりやすい

#### このパターンが適切な場合は？
科学技術演算など、NUMERIC や INTEGER よりも広い範囲の実数値や、浮動小数点による近似値を扱うことが適している場合
- 厳密な10進数の値よりも、値の範囲や計算速度などを重視する場合に利用できる
- 主に集計や近似値計算に利用し、値の完全一致が重要ではないデータの場合に利用できる

#### 解決策
FLOATの代わりにNUMERIC型を利用する。
- NUMERIC(9,2) のように、精度と小数点以下の桁数を指定することで、指定した範囲の10進数を正確に保存できる
- FLOAT が近似値を扱うのに対して、NUMERIC の場合は正確な10進数の値を扱うので, 正確性が要求されるデータに適している
- 保存した値との比較や範囲検索、算術計算などを行う際に、上の長所でより期待した結果を得やすい

#### 🤔 個人的なメモ
dentaku では現在、小数を扱っていないため、小数値の扱いについて深く考えたことがなかった。
そのため、普段のプログラミング習慣から小数ならFLOATと何も考えずにデータ型を選んでしまう可能性があったが、このチャプターを通してFLOATが扱う値は必ずしも正確な小数値ではなく、近似値になる場合があることを知った。
データの扱いによって適切なデータ型の選択が重要だと感じた。

### 第11章　サーティワンフレーバー(31のフレーバー)

#### もともと達成したかった目標
特定のカラムに格納できる値を、あらかじめ定めた固定の値の集合に制限したい。

例) Bugs テーブルの status カラムに、 NEW・IN PROGRESS・FIXED のいずれかだけを格納できるようにしたい

#### アンチパターン
カラムに格納できる値を固定するために、CHECK 制約や ENUM 型を利用するパターン。

例)
```
CREATE TABLE Bugs (
  bug_id BIGINT PRIMARY KEY,
  status VARCHAR(20) CHECK (status IN ('NEW', 'IN PROGRESS', 'FIXED'))
);
```

#### なぜ問題なのか
1. 有効な値の一覧を取得するのが難しい
- CHECK制約・ENUM定義での値は、通常のテーブルとして管理されていない
- そのせいで、カラムにどのような値が設定できるのかという有効な値の一覧を取得したい場合は、データベースのメタデータを調べる必要がある
2. 新しい有効な値を追加するために、データベースのスキーマ変更が必要になる
- ENUM や CHECK 制約で値のみを追加・削除する方法はなく、新しい値のセットで再定義のみ可能
3. 既存の有効な値を削除・変更するのが難しい
- 特定の有効な値を除去したいときは、該当の値を参照している行に対して代替の値として何を設定すればいいのかという問題が発生する
- ALTER TABLE だけでは、除去された値を参照している行の基本値がどう変わるのかが予想できない

#### このパターンが適切な場合は？
相互排他的の値(LEFT/RIGHT, ACTIVE/IN_ACTIVE, ON/OFF)など、今後も値が追加・変更されないことが明確な場合には ENUM が適している場合がある。
また、CHECK 制約では ENUM の具現よりも、カラムに不正な値が格納されることを防ぐための制約として利用するのが適している。 

#### 解決策
固定したい値の集合を専用のテーブルとして管理する。

例) Bugs テーブルの status を管理するために BugStatuses テーブルを作成する
```
CREATE TABLE BugStatuses (
  status VARCHAR(20) PRIMARY KEY,
  is_active BOOLEAN NOT NULL DEFAULT TRUE
);

CREATE TABLE Bugs (
  bug_id BIGINT PRIMARY KEY,
  status VARCHAR(20) NOT NULL,
  FOREIGN KEY (status) REFERENCES BugStatuses(status)
);
```

- 有効な値の一覧を通常のSELECTで簡単に照会できる
- 新しい値を追加するときは、INSERTだけで対応できる
- 値を削除する場合、is_active みたいなフラグカラムを追加で導入することで、論理的な削除として運用が可能になる

### 第12章　ファントムファイル(幻のファイル)

#### もともと達成したかった目標
イメージ・動画・その他の大容量メディアなどのオブジェクトをデータベースと関連付けて保存・管理したい。

例) ユーザがアップロードしたプロフィール画像をユーザ情報と関連付けて管理したい

#### アンチパターン
データベースにはオブジェクトだけではなく、オブジェクトが保存されているファイルパスやURLだけを保存し、実際のファイルはファイルシステムや外部ストレージに保存するパターン。

例) ユーザのイメージファイルの経路を image_path にて管理する  
```
CREATE TABLE UserImages (
  user_id BIGINT PRIMARY KEY,
  image_path VARCHAR(255)
);
```

#### なぜ問題なのか
1. データベースのデータとオブジェクトのライフサイクルが一致しない
- DBとファイルシステムで管理が両分されているので、一方だけが変更・削除される恐れがある(整合性がなくなってしまう)
- アプリケーションコードで上手く操作しないと、行が削除されたオブジェクトが孤立ファイルとして残ってしまう恐れがある
2. ファイルシステム上のオブジェクトはデータベースの原子性の対象にならない
- DBだとトランザクション内でエラーが発生した場合ロールバックを行えるが、ファイル操作は基本的に同じトランザクションで管理されない
- 例えば、あるオブジェクトのパスを参照している行を削除しようとしてトランザクション内で失敗が発生した場合、データ自体は復元できるが一回削除されたオブジェクトを復元するのは難しい
3. データベースのバックアップだけではオブジェクトは復元できない
- データベースとは別に、ファイルシステムに保存されているオブジェクトも追加でバックアップする必要がある
4. データベースだけではオブジェクトの実在を保証できない
- ファイルシステム内に指定されたパスのファイルが実際に存在するかどうかのチェックはデータベースでは不可能
- ファイルの名前やパス変更が行われても、データベースではそれを自動で検知し更新するのはできない

#### このパターンが適切な場合は？
データベースのトランザクションだけに依存するのではなく、システム全体としてデータの整合性を維持する仕組みが上手く設計されている場合。
(必ずこのパターン自体がアンチパターンという意味ではない)

#### 解決策
BLOB などのバイナリ型を利用してオブジェクトをデータベースで直接管理する。
- データベースの行とオブジェクトのライフサイクルを一致させやすい(行の削除・変更などが行われた場合、ファイルシステムの操作が不要になる)
- データベースのトランザクションによって、オブジェクトの変更も含めて管理できる
- ロールバックによって以前の状態に戻しやすい
- データベースのバックアップによって、関連するオブジェクトもまとめてバックアップ可能になる

#### 🤔 個人的なメモ
現在では大容量オブジェクトをオブジェクトストレージに保存し、データベースにはオブジェクトのIDやキーだけを保存する設計が一般的かなと思う。例えば、データベースの行を削除するときにオブジェクトも削除する処理をアプリケーション側で実装したり、削除処理を非同期ジョブとして実行したり、オブジェクトストレージのバージョニングやライフサイクルポリシーを利用したりすることで、データベースとオブジェクトの不整合を防ぐことができる。
要するに、BLOBを使うべきか、外部ストレージを使うべきかが重要ではなく、データのサイズ・アクセス頻度・バックアップ要件・整合性要件などを考慮して、柔軟に対策を選択することが重要だと感じた。

### 第13章　インデックスショットガン

#### もともと達成したかった目標
データベースのパフォーマンスを向上させたい。

#### アンチパターン
明確な根拠や計画を持たずにインデックスを作成・削除するパターン。

例1) インデックスをまったく設定しない
例2) インデックスを過剰に設定する
例3) インデックスを設定したが、クエリがインデックスを利用しない

#### なぜ問題なのか
1. インデックスをまったく設定しない
- インテックスが存在しない場合、条件に一致する行を探すためにテーブル全体を調べる必要がある
- テーブルのデータ量が増えるほど、フルテーブルスキャンのコストも高くなる
- そのため、インテックスの更新コストが高いからインテックスを作らないという判断は適切ではない

2. インデックスを過剰に設定する
- インデックスを利用するクエリを実行する場合のみ、インデックスのメリットを活かすことができるが、実際には使用する予定のないインデックスを作成する場合
- 不要なインデックスを作るほど、行の更新などで無駄なコストが発生する

3. インデックスを設定したが、クエリがインデックスを利用しない
- クエリの書き方によってインデックスを利用しない場合がある

つまり、上記の問題の根本的な原因は、クエリや実行計画などを行わずに根拠無しでインデックスを触ろうとする行動から起因する。

#### このパターンが適切な場合は？
例えば、書き込みが非常に多く検索処理がほとんどないテーブルであれば、インデックスを最小限にすることには合理的な理由がある。
逆に、読み取りが非常に多く特定の検索クエリが頻繁に実行されるのであれば、複数のインデックスを作成することが合理的な場合もある。

明確な根拠があってインデックスを作成・削除するケースは適切という意味。

#### 解決策
インデックスを含めるべきか削除すべきかを判断するために、MENTORの手順でデータベースを分析する。
1. Measure(測定)
- 実際にどのクエリが遅いのかを測定し識別する
2. Explain(実行計画)
- 問題になっているクエリに対して実行計画を行う
3. Nominate(候補選定)
- 改善に約立つ可能性のあるインデックスを候補として選定する
4. Test(テスト)
- 候補として選定したインデックスを実際に作成し、本当にパフォーマンスが改善するのかを検証する
5. Optimize(最適化)
- テスト結果をもとに、インデックスやデータベースの設定をさらに改善する
- インテックスをカラムを見直すか、メモリー設定を調整するかなど
6. Rebuild(再構築)
- 時間の経過によってインデックスの状態が悪くなっていないかを確認し、定期的にメンテナンスをする

## 全体的な感想
...

## 以下は各チャプターごとのテンプレート
### 第N章　...

#### もともと達成したかった目標
データベースを利用して解決したかったそもそもの問題について簡略に紹介

#### アンチパターン
目標に対して実際に実行してはいけないアンチパターンの詳細

#### なぜ問題なのか
起きえる問題を項目別にまとめ

#### このパターンが適切な場合は？
例外のケースのまとめ

#### 解決策
具体的な解決方法...
- (＋「なぜ問題なのか」で羅列された問題点がどう解決するかも簡略にまとまれるなら記載)

#### 🤔 個人的なメモ(任意)
このアンチパターンの内容での個人的な感想など
