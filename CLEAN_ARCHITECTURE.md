# Clean Architecture

Clean Architecture を読み、その内容や感想などを記録として残す。
29, 33, 34 章については、内容のまとめよりアーキテクチャの原則を把握するのに集中するために省略した。

※ 韓国語版で読書したため、日本語版での用語と一部差異がある可能性がある。

## 各章ごとの内容まとめ・個人的なメモ

### 第1章　設計とアーキテクチャ
設計(=アーキテクチャ)を常に深く考慮し、時には遠回りに見えても、将来のコストを最小化する姿勢を持つべきである。
- 🤔: 「急がば回れ」という言葉通り、目先の期限などに襲われ品質(アーキテクチャ)を妥協することが、結局後でどれほど大きな負債として返ってくるかを警戒しなければならないと感じた。

### 第2章　2つの価値のお話
振る舞い(機能実装) vs アーキテクチャ(構造)。ソフトウェアアーキテクトは、アーキテクチャの重要性のために絶えず戦わなければならない。
- 🤔: 変更が容易な構造を維持することこそが、アーキテクトの真の価値であることを学んだ。

------------------------------------------------------------------
以下は、プログラミングパラダイムについての紹介である。

### 第3章　パラダイムの概要
本書におけるパラダイムとは、プログラミングを行う方法(=開発者を制約するルールの集合)を意味する。
構造化プログラミング・オブジェクト指向プログラミング・関数型プログラミングは、それぞれ制御の流れ、依存関係・データ管理に制約をかけ、アーキテクチャの基盤となる。
- 🤔: パラダイムが「何ができるか」に集中するのではなく、「何をすべきではないか」という制約に集中しているという点が新鮮だった。

### 第4章　構造化プログラミング
goto文の禁止により、プログラムの流れをわかりやすく安全にしたパラダイム。
順次・分岐・反復といった定型化された構造を通じてコードの予測可能性が高くなり、テストしやすい単位へと自然に分割されるようになった。

### 第5章　オブジェクト指向プログラミング
オブジェクト指向の本質は、ポリモーフィズムによる依存関係逆転の可能性にある。インターフェースを通じて、高レベルのポリシーモジュールが低レベルの詳細から独立することが可能になった。
- 🤔: 単にクラスや継承を使うことがオブジェクト指向プログラミングではなく、核心は制御の流れと依存関係の方向を分離し、「依存関係を制御する」ことにあるのだと理解した。

### 第6章　関数型プログラミング
状態を変化させないという「不変性」の制約を意味する。副作用を最小化することで、並行性の問題やプログラムの予測可能性を高めることができる。
- 🤔: 実務で可変変数を最小化する理由も、これと関係することだねと納得した。

--------------------------------------------------------

以下は、ソフトウェア設計の原則についての内容である。

### 第7章　SRP：単一責任の原則
モジュールを変更する理由は、たった1つであるべきであり、それは特定のアクターのためだけに存在すべきであることを意味する。
異なるアクターの要求が1つのところに混在しないように、構造を分離しなければならない。
- 🤔: クラスを分離する基準がコードの機能的な類似性ではなく、誰がこのコードを変えようとするのかであるべきだという点が印象的だった。

### 第8章　OCP：オープン・クローズドの原則
ソフトウェアの構成要素は拡張に対して開いていなければならないが、修正に対しては閉じていなければならないという原則。
上位のポリシーは、詳細の変更による影響を受けないように保護されるべきである。
- 🤔: 保守性の観点からも、新しい機能を追加する際に既存のコードを触らなくて済む構造を作ることが非常に重要だと感じた。

### 第9章　LSP：リスコフの置換原則
親タイプを使用する場所で、子タイプを入れ替えても問題が起きないようにすべきだという原則。
子が親クラスのルールを壊してしまうと、上位レイヤーまで汚染され、アーキテクチャが崩壊する。

### 第10章　ISP：インターフェイス分離の原則
クライアントが利用しないメソッドへの依存を強制してはいけない。
1つの巨大なインターフェースより、役割に応じた多数の小さなインターフェースの方が結合度を下げるのに有利である。

### 第11章　DIP：依存関係逆転の原則
高レベルのモジュールが低レベルのモジュールに依存するのではなく、インターフェースに依存すべきだという原則。
具象が抽象に依存するように、構造を逆転させることを意味する。

----------------------------------------------------------

以下からは、コンポーネントの原則についての内容。

### 第12章　コンポーネント
ここでのコンポーネントとは、ビルドやデプロイする際に1つの単位として扱われる塊のこと。
適切に設計されたコンポーネントは、独立して開発・デプロイできる必要がある。
- 🤔: 今まではモジュール分割をコード整理の一環として考えていたが、デプロイ戦略も考慮する重要な要素であることを学んだ。

### 第13章　コンポーネントの凝集性
どのクラスを1つのコンポーネントにまとめるかを意味。
凝集度が高いということは、コンポーネントが1つの明確な役割を遂行し、変更が容易で依存関係がきれいになることに直結する。
逆に凝集度が低いということは、機能が無関係なものが混在していて、変更が難しくなることを意味する。

凝集度を上げるとめの3原則は以下の通り
- 再利用・リリース等価の原則: 再利用する際に、コンポーネントを丸ごと1つ使用し、デプロイもその単位で遂行する。1つの理由でバージョンが上がり、1つの理由で再利用をしなければならないということ。
- 閉鎖性共通の原則: 同じ理由で変更されるクラスは同じコンポーネントに存在しなければならない。
- 全再利用の原則: 一緒に使わない機能はお互いに離れていなければならない。 

上記の3つの原則は互いに相反する面が存在するため、アーキテクチャ設計時に正しいバランスを取る努力をしなければならない。
- 🤔: 実際に上記の3つの原則の間でバランスを取ることがどんなに難しいことなのかが、今の時点では想像できない…

### 第14章　コンポーネントの結合
コンポーネント同士が結合される際、どのような方式で依存すれば保守が容易になるか、規則を立てることに関連した内容。

次のような原則が提示されていた。
- 非循環依存関係の原則: 依存関係グラフに循環があってはならない。
- 安定依存の原則: 頻繁に変更されるコンポーネントが、変更の難しい安定したコンポーネントに依存しなければならない。
- 安定度抽象位の原則: 安定したコンポーネントほど抽象的でなければならない。

つまり、依存関係サイクルを作らず、不安定なものが安定したものを依存するようにし、安定したものは抽象的でなければならないということ。

-----------------------------------------------------------------
以下からは、アーキテクチャに関する内容。

### 第15章　アーキテクチャとは？
アーキテクチャとは、システムにおいて変更しにくくコストがかかる決定の集合を意味する。
アーキテクチャの最終的な目標は、システムのライフサイクルに関連するコストを最小限にし、生産性を最大化すること。
これを達成するためには、方針を最も核心的な要素として識別し、詳細を方針から切り離すという原則を守る姿勢が必要である。
- 🤔: 今までの個人的な作業では、フレームワークやDBなどの詳細を先に決定して作業を開始していたが、詳細に関する決定を最大限に先送りにすることが保守コストが下がるという内容だったので、反省することになった。

### 第16章　独立性
良いアーキテクチャは、ユーズケース・運用・開発・デプロイの側面で独立性を保証しなければならない。
階層(垂直的)だけでなく、ユーズケース(水平的)単位でも分離し、お互いに干渉することなく拡張が可能でなければならない。

### 第17章　バウンダリー：境界線を引く
境界とは、方針が詳細(DB, UI など)を知らないようにする線を意味する。コアロジックを外部要素から独立させ、プラグインアーキテクチャの形にしなければならない。
- 🤔: またアーキテクチャについてよく知ってはいないが、プラグインアーキテクチャだけが唯一の答えなのだろうかという疑問は持った。

### 第18章　境界の解剖学
境界を作る方式には、弱い境界〜強い境界の順に非常に多様で、状況に合った適切な強度を選択しなげればならない。
- 🤔: 適切なトレードオフの地点を探すことが非常に難しいことのように感じられた。

### 第19章　方針とレベル
ここでレベルとは、入力・出力からの距離を意味する。入出力に近いほど低レベル、業務のルールに近いほど高レベルである。低レベルのコンポーネントは高レベルのコンポーネントにプラグインされなければならない。

### 第20章　ビジネスルール
エンティティは業務のコアルールを包含し、ユーズケースはアプリケーションに特化した業務ルールを包含する。これらはシステムの核心であるため、外部から隔離されなければならない。
- 🤔: フレームワークロジックとビジネスロジックが混在してはいけない。

### 第21章　叫ぶアーキテクチャ
アーキテクチャを見るだけで、このシステムが何をするシステムなのかすぐわからなければならないという意味。
フレームワークなどの詳細が主人公になってはならない。
- 🤔: 学生時代からフレームワークを第1順位として考えてきた自分について反省することになった。

### 第22章　クリーンアーキテクチャ
エンティティ・ユースケース・アダプター・フレームワーク階層に分け、依存関係は常に外側(低レベル)から内側(高レベル)に向かわなければならない。

### 第23章　プレゼンターとHumble Object
- プレゼンター：テストしやすいコードの塊
- ハンブルオブジェクト：テストしにくいコードの塊
これらを分離して境界でのテストカバレッジを高めるパターンである。
- 🤔: 全体の本のコンテクストで、この章で話したいことが何かについて理解ができていなかった。

### 第24章　部分的な境界
境界は重要だが、現実的に常に完璧な境界を作ることは無理。
完璧な境界の完成にはコストが多くかかり、結局のところ境界が不必要になる場合もある。

したがって代替案として、部分的な境界の必要性が浮上。後で分離が決定されても対応しやすくしようというのが部分的な境界の意味。例えば、将来に備えてインターフェースだけを事前に定義しておくなどの方式。

### 第25章　レイヤーと境界
境界はシステムの色々のところに存在し、単なる水平的なレイヤーの分離を超えて、複雑な形になることもある。
レイヤーと境界は同一ではなく、1つのレイヤーの内部でも境界が必要な場合もある。
アーキテクトはこれを継続的に監視し、設計しなければならない。
- 🤔: アーキテクチャの設計は一度行えば終わりでなく、継続的な行動が必要であることを知った。

### 第26章　メインコンポーネント
メインはすべてのコンポーネントを生成・組み立てし、依存関係を注入する、最も汚い最外部コンポーネントである。システムの進入点として、すべての詳細を決定する。

### 第27章　サービス：あらゆる存在
サービス間の分離はデプロイ単位を分けるだけであり、サービスの分離がすなわち境界の分離を意味するわけではない。サービスはアーキテクチャを代替できない。

### 第28章　テスト境界
テストはシステムの一部で、システムの境界の外からユースケースを検証する役割をする。テストコードも保守性を考慮して設計しなければならない。
- 🤔: 間違ったテスト設計はシステムの保守においてコストの増加を招く可能性もあると感じた。

### 第30章　データベースは詳細
データは重要だが、データベースは詳細(低レベル)である。
ビジネスロジックはデータモデルが保存される方式(SQL, NoSQL など)について知ってはいけない。

### 第31章　ウェブは詳細
ウェブは入出力装置としてGUIと同様に詳細(低レベル)に該当する。
ビジネスロジックはウェブフレームワークの存在有無とは関係せずに、独立に動作しテストされなければならない。

### 第32章　フレームワークは詳細
フレームワークに依存する際は、適度に依存しなければならない。
フレームワークは詳細であるため、核心コードは依然としてフレームワークについて知ってはならない。
- 🤔: 特定のフレームワーク技術に精通した開発者になることも重要だが、何よりも重要なのは、根本的な設計能力を備えたエンジニアにならなければならないということを感じた。

## 全体的な感想
TODO
